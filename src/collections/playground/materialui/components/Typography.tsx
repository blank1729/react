import { Typography } from "@mui/material";

const TypographyComponent = () => {
  return (
    <Typography variant="h2" component="h1" gutterBottom>
      h2 size with h1 element and margin on the bottom
    </Typography>
  );
};

export default TypographyComponent;
